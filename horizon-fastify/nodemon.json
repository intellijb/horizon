{
  "watch": ["src/**/*.ts"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "tsx --tsconfig tsx.config.json src/server.ts",
  "env": {
    "NODE_ENV": "development",
    "NODE_OPTIONS": "--experimental-specifier-resolution=node --import tsx/esm"
  }
}